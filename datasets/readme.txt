These datasets are shared in common between many of the notebooks. Due to size constraints, I needed to delete the data in individual workspaces and pooled the common ones here.

X_train_id, X_test_id, y_train_id, and y_test_id are all directly from the LoGoFunc repo.